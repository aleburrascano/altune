# Active staging blue/green upstream. Imported by the altune-staging.duckdns.org
# site block and the internal :8082 overseer listener in ../Caddyfile, and
# rewritten by the staging blue-green deploy
# (#1491), the same way deploy/caddy/upstream.conf works for prod. Committed with
# an initial blue pointer so the prod Caddy import resolves on first bring-up;
# after that the deploy owns this file.
reverse_proxy altune-staging-go-api-blue:8000
