"""Application layer — use cases + port interfaces.

Depends on `domain/` only. Defines ports (Protocol / ABC) that adapters implement.
See .claude/rules/application-layer.md.

Per-context use cases added as packages here as features arrive:
- catalog/, library/, playback/, metadata/

Day-1 scaffold: intentionally empty.
"""
