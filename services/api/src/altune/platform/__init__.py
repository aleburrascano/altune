"""Platform layer — config, DI wiring, logging, observability.

This is the layer that WIRES the others together. It depends on everything; nothing depends on it
except the entrypoint (`app.py`).
"""
