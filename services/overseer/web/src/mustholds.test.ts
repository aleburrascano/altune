import { describe, it } from "vitest";

describe("overseer revamp must-holds owned by later tickets", () => {
  it.skip("after a stream error, the next frame received sets the connection pill back to live (lands in [Task]: overseer web connection status tells the truth, #2373)", () => {});

  it.skip("with no frame for about 3 server ticks the UI shows stalled, never live (lands in [Task]: overseer web connection status tells the truth, #2373)", () => {});

  it.skip("no page scrolls sideways at 360px wide (lands in [Task]: overseer web responsive shell with phone nav, #2372)", () => {});

  it.skip("every nav item and control is reachable and visibly focused by keyboard (lands in [Task]: overseer web responsive shell with phone nav, #2372)", () => {});

  it.skip("no panel file uses inline style={} (lands in [Task]: overseer web bans inline styles in panels, #2390)", () => {});

  it.skip("no file uses dangerouslySetInnerHTML (lands in [Task]: overseer web lint adds hooks, a11y and unsafe-html rules, #2369)", () => {});

  it.skip("go snapshot and series json match web/src/types.ts (lands in [Task]: overseer contract test pins go snapshot and series json to the web types, #2387)", () => {});

  it.skip("the gzipped JS bundle stays under 250 KB (lands in [Task]: overseer web ci enforces a 250 KB gzipped bundle budget, #2388)", () => {});
});
