{
  "name": "altune",
  "version": "0.0.0",
  "private": true,
  "description": "Altune — music manager. Repo root: git-hook tooling only (husky + commitlint). The mobile app is a standalone npm package under apps/mobile (see ADR-0016); the Go service lives under services/go-api.",
  "scripts": {
    "prepare": "husky",
    "lint:commits": "commitlint --from=HEAD~10 --to=HEAD --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  },
  "engines": {
    "node": ">=20"
  }
}
