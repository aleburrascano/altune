describe('mh04: the web session survives a reload and sign-out clears it', () => {
  it.skip('keeps a signed-in user signed in across a reload, and leaves no Supabase session in localStorage after sign-out (un-skipped by "web session persists across reloads")', () => {});
});
