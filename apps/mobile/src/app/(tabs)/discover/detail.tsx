/**
 * /discover/detail — detail screen within the discover stack.
 *
 * Renders the same DetailScreen component; the stack navigator allows
 * unlimited push depth (discover → artist → album → track → ...) with
 * natural back-button behavior.
 */

import { DetailScreen } from '../../../features/detail/ui/DetailScreen';

export default DetailScreen;
