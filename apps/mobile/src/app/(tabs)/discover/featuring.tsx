/**
 * /discover/featuring — "everything featuring X" within the discover stack, so
 * tapping a featured artist on a discover-stack track detail keeps the back
 * button returning to that detail. Same screen as /library/featuring.
 */

import { FeaturingScreen } from '../../../features/library/ui/FeaturingScreen';

export default FeaturingScreen;
