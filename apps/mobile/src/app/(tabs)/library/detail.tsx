/**
 * /library/detail — detail screen within the library stack.
 *
 * Same DetailScreen component as discover/detail; nested in the library
 * stack so back-button returns to the library screen.
 */

import { DetailScreen } from '../../../features/detail/ui/DetailScreen';

export default DetailScreen;
