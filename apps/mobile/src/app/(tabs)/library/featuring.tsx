/**
 * /library/featuring — "everything featuring X": the user's saved tracks that
 * credit a featured artist, reached by tapping a featured-artist name on a
 * detail screen. Params: name, mbid?, deezer_id?.
 */

import { FeaturingScreen } from '../../../features/library/ui/FeaturingScreen';

export default FeaturingScreen;
