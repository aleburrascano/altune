# Altune — AltStore / SideStore source branch

This branch holds only `apps.json`, the sideload source manifest.
It is updated automatically by the `Release iOS` workflow on every `v*` tag.

Add this source URL in AltStore/SideStore:

    https://raw.githubusercontent.com/aleburrascano/altune/altstore/apps.json
